#:schema https://json.schemastore.org/pyproject.json

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[dependency-groups]
lint = ["ruff>=0.9.5"]
types = ["types-jsonschema>=4.23.0.20241208", "pyright>=1.1.393"]

[project]
name = "simple-resume"
version = "1.0.0"
description = "CLI tool to build a resume using a JSON Resume file"
authors = [{ name = "Sebastián Altamirano" }]
dependencies = [
	"Babel>=2.17.0",
	"Flask>=3.1.0",
	"email-validator>=2.2.0",
	"jinjax[whitenoise]>=0.48",
	"jsonschema>=4.23.0",
	"livereload>=2.7.1",
	"playwright>=1.50.0",
	"typer>=0.15.1",
	"pydantic>=2.10.6",
	"pydantic-extra-types[phonenumbers,pycountry,semver]>=2.10.2",
	"requests>=2.32.3",
	"tldextract>=5.1.3",
]
requires-python = "==3.12.*"
readme = "README.md"
license = { text = "MIT" }

[project.scripts]
simple-resume = "simple_resume.__main__:app"

[tool.pdm]
distribution = true

[tool.pdm.scripts]
extract-messages = "pybabel extract . -F simple_resume/translations/babel.cfg -o simple_resume/translations/messages.pot"
initialize-messages = "pybabel init -i simple_resume/translations/messages.pot -d simple_resume/translations -l"
update-messages = "pybabel update -i simple_resume/translations/messages.pot -d simple_resume/translations"
compile-messages = "pybabel compile -d simple_resume/translations"
format.composite = [
	"ruff check --select I --fix",             # Sort imports
	"ruff format",
	"npx prettier --write --ignore-unknown .",
]
lint.composite = ["ruff check", "pyright"]
lint.env.PYRIGHT_PYTHON_PYLANCE_VERSION = "latest-release"
frontman = "./scripts/run_frontman.sh"
# Hooks
post_install = { composite = ["frontman install"] }
pre_build = { composite = ["compile-messages"] }

[tool.pyright]
include = ["simple_resume"]
typeCheckingMode = "strict"

[tool.ruff]
indent-width = 4
line-length = 100
src = ["simple_resume"]

[tool.ruff.format]
line-ending = "lf"

[tool.ruff.lint]
ignore = ["D107", "ISC001"]
select = [
	"A",
	"ANN",
	"ARG",
	"ASYNC",
	"B",
	"BLE",
	"C4",
	"C90",
	"D",
	"DTZ",
	"E",
	"EM",
	"ERA",
	"F",
	"FA",
	"FBT",
	"FIX",
	"FURB",
	"I",
	"ICN",
	"INP",
	"INT",
	"ISC",
	"N",
	"PERF",
	"PIE",
	"PLC",
	"PLE",
	"PLR",
	"PLW",
	"PTH",
	"Q",
	"RET",
	"RUF",
	"SIM",
	"SLF",
	"SLOT",
	"T10",
	"T20",
	"TID",
	"TRY",
	"UP",
	"W",
	"YTT",
]

[tool.ruff.lint.isort]
required-imports = ["from __future__ import annotations"]

[tool.ruff.lint.pydocstyle]
convention = "google"
