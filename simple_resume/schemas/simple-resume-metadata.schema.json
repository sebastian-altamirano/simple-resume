{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"additionalProperties": false,
	"properties": {
		"language": {
			"description": "The language of the resume, as a language tag, for example: 'en', 'en_US'.",
			"type": "string"
		},
		"template": {
			"description": "The name of the template to be used to generate or serve the resume.",
			"type": "string"
		}
	},
	"title": "Simple Resume metadata",
	"type": "object"
}
